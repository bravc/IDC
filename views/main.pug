extends layout

block content 
	h1 Registered Users
	if !users.empty
		ul.list-group
			each user, i in users
				li.list-group-item
					a(href="/users/"+ user._id)=user.name
	else
		p1 No users online!