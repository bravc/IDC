extends layout

block content 





	.container
		.row
			h3 New Post
		.row
			.col-md-6
				.widget-area.no-padding.blank
					.status-upload
						form(method='POST', action='/posts/new')
							textarea(name='body', placeholder='What are you doing right now?')
							ul
								li
									a(title='', data-toggle='tooltip', data-placement='bottom', data-original-title='Audio')
										i.fa.fa-music
								li
									a(title='', data-toggle='tooltip', data-placement='bottom', data-original-title='Video')
										i.fa.fa-video-camera
								li
									a(title='', data-toggle='tooltip', data-placement='bottom', data-original-title='Sound Record')
										i.fa.fa-microphone
								li
									a(title='', data-toggle='tooltip', data-placement='bottom', data-original-title='Picture')
										i.fa.fa-picture-o
							button.btn.btn-success.green(type='submit')
								i.fa.fa-share
								|  Submit
					// Status Upload
				// Widget Area


	br
	br





	if !users.empty
		each user in users
			each post in user.posts
				.container
					.row
						.col-sm-1
							.thumbnail
								img.img-responsive.user-photo(src=user.profilePic)
							// /thumbnail
						// /col-sm-1
						.col-sm-5
							.panel.panel-default
								.panel-heading
									strong #{user.name}
									span.text-muted= "  " + post.date
								.panel-body  #{post.content}
								// /panel-body
							// /panel panel-default
						// /col-sm-5		

